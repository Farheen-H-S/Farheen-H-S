# README Generator

This script parses the content definitions from `content/CONTENT.md` and compiles them into the final profile `README.md` using GitHub-compatible layout tables.

## Usage

Rerun this script whenever you update your profile content:

```bash
python scripts/generate_readme.py
```
